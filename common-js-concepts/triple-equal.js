// always use ===
// equal comparison doesn't work for non-primitive
const first = 2;
const second = true;
if(first == second){
    console.log('values are equal');
}
else{
    console.log('values are not equal');
}
// triple equal(===) check value and type && double equal(==) check value only

// type coercion
// type conversion
// type casting